
<section class="contact-section">
    <div class="contact-card">
        <div class="contact-details">
            <div class="contact-hours">
                <span class="icon"><i class="fa-solid fa-clock"></i></span>
                <div>
                    <span class="schedule">Horarios de atención</span><br>
                    <span>Lunes a Viernes: <b>8:00 - 13:00</b> / <b>16:00 - 20:00</b></span><br>
                    <span>Sábados: <b>8:00 - 13:00</b></span>
                </div>
            </div>
            <div class="contact-links">
                <a href="tel:+543434311060" class="contact-link" title="Llamar">
                    <span class="icon"><i class="fa-solid fa-phone"></i></span> <span>+54 343 431-1060</span>
                </a>
                <a href="mailto:laestrellapna@gmail.com" class="contact-link" title="Enviar Email">
                    <span class="icon"><i class="fa-solid fa-envelope"></i></span> <span>laestrellapna@gmail.com</span>
                </a>
                <a href="https://www.instagram.com/farmacia.laestrella/" class="contact-link" target="_blank" rel="noopener noreferrer" title="Instagram">
                    <span class="icon"><i class="fa-brands fa-instagram"></i></span> <span>@farmacia.laestrella</span>
                </a>
            </div>
        </div>
        <div class="contact-map-container">
            <iframe src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d504.4309605287784!2d-60.533636997493474!3d-31.732458807257764!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x95b4536a366fbfd3%3A0x4131dff9c90c9872!2sFarmacia%20La%20Estrella!5e0!3m2!1ses!2sar!4v1749677367880!5m2!1ses!2sar" allowfullscreen loading="lazy" referrerpolicy="no-referrer-when-downgrade" class="contact-map"></iframe>
        </div>
    </div>
</section>

<style>
    .contact-section {
        max-width: 1240px;
        width: 100%;
        padding: 0 20px;
        margin: 40px auto 170px;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .contact-card {
        display: flex;
        border-radius: 25px;
        box-shadow: 0 6px 32px 0 var(--color-shadow);
        width: 100%;
        overflow: hidden;
        border: 2px solid var(--color-primary-lighter);
    }

    .contact-details {
        display: flex;
        flex-direction: column;
        justify-content: center;
        gap: 24px;
        background: var(--color-white);
        flex: 1 1 350px;
        padding: 48px;
        border-right: 2px solid var(--color-primary-lighter);
    }

    .contact-hours {
        display: flex;
        gap: 16px;
        font-size: 1.1rem;
        color: var(--color-text);
        background: var(--color-primary-lighter);
        border-radius: 25px;
        padding: 16px 24px;
        box-shadow: 0 2px 8px 0 var(--color-shadow);
        text-wrap: balance;
    }

    .schedule {
        font-weight: 600;
        font-size: 1.2rem;
        color: var(--color-accent);
    }

    .contact-links {
        display: flex;
        flex-direction: column;
        gap: 16px;
        margin-top: 8px;
    }

    .contact-link {
        display: flex;
        align-items: center;
        gap: 16px;
        font-size: 1.1rem;
        color: var(--color-text);
        background: var(--color-primary-lighter);
        border-radius: 25px;
        padding: 16px 24px;
        transition: all 0.2s ease;
        font-weight: 500;
        box-shadow: 0 1px 4px 0 var(--color-shadow);
    }

    .contact-link:hover {
        background: var(--color-accent);
        color: var(--color-text-on-primary);
        box-shadow: 0 8px 20px var(--color-shadow-hover);
    }

    .icon {
        font-size: 1.4rem;
    }

    .contact-map-container {
        flex: 2 1 600px;
        padding: 0;
    }

    .contact-map {
        width: 100%;
        height: 100%;
        border: none;
        border-radius: 0;
    }

    @media (max-width: 1024px) {
        .contact-section {
            width: 95%;
            margin: 20px auto 40px;
            padding: 0;
        }

        .contact-card {
            flex-direction: column;
        }

        .contact-details {
            border-right: none;
            border-bottom: 2px solid var(--color-primary-lighter);
            padding: 32px;
            gap: 16px;
        }

        .contact-hours {
            font-size: 1rem;
            padding: 12px 16px;
        }

        .schedule {
            font-size: 1.1rem;
        }

        .contact-link {
            font-size: 1rem;
            padding: 12px 16px;
        }

        .icon {
            font-size: 1.2rem;
        }

        .contact-map-container {
            flex: none;
            height: 350px;
        }
    }
</style>